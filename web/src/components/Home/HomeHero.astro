<section id="home" class="hero-showcase md:min-h-fit pt-40 mb-48">
    <div class="hero-pattern-overlay" aria-hidden="true">
        <span class="pattern-band pattern-band--one"></span>
        <span class="pattern-band pattern-band--two"></span>
        <span class="pattern-grid"></span>
    </div>
    <div class="container mx-auto hero-content">
        <div
            class="flex flex-col max-w-xl md:max-w-none md:flex-row justify-around md:justify-between mx-auto my-14 md:my-18 space-y-10"
        >
            <div class="flex justify-center flex-col">
                <div class="">
                    <h1
                        class="heading text-5xl md:text-7xl xl:text-8xl font-bold"
                    >
                        IEEE
                    </h1>
                    <h1
                        class="heading text-5xl md:text-7xl xl:text-8xl font-bold"
                    >
                        IIIT Delhi
                    </h1>
                    <h2
                        class="heading2 text-3xl md:text-4xl font-bold text-gray-500 join"
                    >
                        student branch
                    </h2>
                </div>
                <div
                    class="flex flex-row space-x-7 items-center join mt-16 xl:mt-36"
                >
                    <p class="text-2xl">Join our team</p>
                    <a href="https://forms.gle/5xoKNbj6moQpRn2o9">
                        <button
                            class="fancyBtn applyBtnTop heading2 rounded-lg border-1 px-9 py-3 leading-7 text-xl text-white"
                        >
                            apply
                        </button>
                    </a>
                </div>
            </div>
            <div>
                <img
                    src="/hero-img.webp"
                    alt="home"
                    class="md:max-w-sm xl:max-w-xl xl:max-h-[633px] rounded-xl"
                />
            </div>
        </div>
    </div>
</section>

<style>
    .hero-showcase {
        position: relative;
        overflow: hidden;
        border-radius: 1.4rem;
    }

    .hero-content {
        position: relative;
        z-index: 3;
    }

    .hero-pattern-overlay {
        position: absolute;
        inset: 0 4%;
        pointer-events: none;
        z-index: 2;
        opacity: 0.55;
    }

    .pattern-band {
        position: absolute;
        border-radius: 999px;
        backdrop-filter: blur(0.5px);
    }

    .pattern-band--one {
        width: 46%;
        height: 40%;
        left: -8%;
        top: 0;
        background: repeating-linear-gradient(
            130deg,
            rgba(130, 149, 255, 0.24) 0,
            rgba(130, 149, 255, 0.24) 9px,
            rgba(130, 149, 255, 0.04) 9px,
            rgba(130, 149, 255, 0.04) 20px
        );
        animation: heroPatternSweepA 12s ease-in-out infinite alternate;
    }

    .pattern-band--two {
        width: 40%;
        height: 34%;
        right: -2%;
        bottom: 7%;
        background: repeating-linear-gradient(
            -130deg,
            rgba(120, 236, 205, 0.22) 0,
            rgba(120, 236, 205, 0.22) 8px,
            rgba(120, 236, 205, 0.04) 8px,
            rgba(120, 236, 205, 0.04) 18px
        );
        animation: heroPatternSweepB 14s ease-in-out infinite alternate;
    }

    .pattern-grid {
        position: absolute;
        inset: 6% 12% 12% 20%;
        background-image:
            linear-gradient(rgba(117, 140, 255, 0.14) 1px, transparent 1px),
            linear-gradient(90deg, rgba(117, 140, 255, 0.14) 1px, transparent 1px);
        background-size: 36px 36px;
        mask-image: radial-gradient(circle at center, #000 42%, transparent 90%);
        animation: heroGridDrift 16s linear infinite;
    }

    @keyframes heroPatternSweepA {
        from {
            transform: translate3d(-3%, 0, 0) rotate(-3deg);
        }
        to {
            transform: translate3d(4%, 2%, 0) rotate(2deg);
        }
    }

    @keyframes heroPatternSweepB {
        from {
            transform: translate3d(2%, 2%, 0) rotate(3deg);
        }
        to {
            transform: translate3d(-3%, -2%, 0) rotate(-2deg);
        }
    }

    @keyframes heroGridDrift {
        from {
            transform: translate3d(0, 0, 0);
            opacity: 0.62;
        }
        to {
            transform: translate3d(40px, 24px, 0);
            opacity: 0.92;
        }
    }

    @media (max-width: 768px) {
        .hero-pattern-overlay {
            inset: 0;
            opacity: 0.42;
        }

        .pattern-grid {
            inset: 8% 4% 16% 8%;
            background-size: 28px 28px;
        }
    }

    @media (prefers-reduced-motion: reduce) {
        .pattern-band--one,
        .pattern-band--two,
        .pattern-grid {
            animation: none;
        }
    }
</style>
